from __future__ import unicode_literals
import multiprocessing

bind = "127.0.0.1:{{ web_gunicorn_port }}"
workers = multiprocessing.cpu_count() * 2 + 1
loglevel = "error"
proc_name = "{{ project_name }}"
